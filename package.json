{
  "name": "worker",
  "version": "1.0.0",
  "description": "Worker for Huoltolaskuri.fi -app (handles example consuming AMQ and creates PDF documents to S3 bucket)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Niiles Kari",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.8",
    "nodemailer": "^7.0.5"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
